#include <string.h>
#include "main.h"
/**
 * int _strlen - function that return the lenght of a string
 * @s: the string
 * return: the lenght of the string
*/
int _strlen(char *s)
{
return strlen(*s);
}

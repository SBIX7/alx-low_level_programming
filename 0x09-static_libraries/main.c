#include "main.h"  // Inclure le fichier d'en-tête contenant les prototypes des fonctions

int main(void)
{
    char quote[] = "\"At the end of the day, my goal was to be the best hacker\"\n\t- Kevin Mitnick";

    // Utilisation de la fonction _puts pour afficher la citation
    _puts(quote);

    return (0);
}


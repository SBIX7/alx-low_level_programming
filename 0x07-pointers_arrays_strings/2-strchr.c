#include <string.h>
#include "main.h"

/**
 * _strchr - Function that return the first occurence of char c.
 * @s: The string.
 * @c: The character to find.
 *
 * Return: The first occurance of char c in s.
 */
char *_strchr(char *s, char c)
{
return (strchr(s, c));
}
